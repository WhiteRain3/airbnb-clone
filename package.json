{
  "name": "airbnb-clone-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.0",
    "path": "^0.12.7",
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^7.2.4"
  }
}